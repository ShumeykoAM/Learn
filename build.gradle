//Каждая задача гредла - может наследоваться от классов

//Отнаследуемся от класса Copy и обращаемся к методам базового класса
task copyFiles(type: Copy) {
    from 'resources'
    into 'target'
    include ('**/*.xml', '**/*.txt', '**/*.properties')
}